import { Service } from 'typedi';
import { BaseRepository } from './base';
import {{name}}Entity from '../entities/{{nameSnakeCase}}';

@Service()
export default class {{name}}Repository extends BaseRepository<{{name}}Entity> {
  constructor() {
    super({{name}}Entity);
  }
}